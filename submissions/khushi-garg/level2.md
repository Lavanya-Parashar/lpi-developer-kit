# Level 2 Learnings – Khushi Garg

## Test Client Output

* Ran `npm run test-client` successfully
* Output showed all tools working correctly:

[PASS] smile_overview
[PASS] query_knowledge
[PASS] get_case_studies
[PASS] get_insights
[PASS] list_topics
[PASS] smile_phase_detail
[PASS] get_methodology_step

## LLM Output

* Used LLaMA3 model via Ollama
* The model explained digital twin concepts using the SMILE methodology
* It demonstrated how real-world systems can be represented digitally and analyzed step by step

## Model Choice

* I chose the LLaMA3 model because it runs locally without requiring external APIs
* It provides efficient and fast responses and integrates well with the project setup
  
## Reflection (SMILE)

* I was surprised to see how effectively the model explained digital twin concepts using structured methodology
* I learned how AI agents interact with tools and generate meaningful responses
* I found it interesting how local models can work efficiently without external APIs in real-world scenarios

## How I Did It

To complete Level 2, I followed a structured process to set up and run the project locally.

First, I opened the terminal and navigated to the project directory. If the repository was not already available, I cloned it from GitHub and moved into the folder.

Then, I installed all dependencies using `npm install`, ensuring that all required packages were properly set up.

After that, I built the project using `npm run build` to prepare it for execution.

Finally, I ran the test client using `npm run test-client`. This step confirmed that all tools were working correctly, as all outputs showed a PASS status.

## Result

The project executed successfully without errors, and all tools passed the test client checks. This confirmed that my setup was correct and the system was functioning properly.
